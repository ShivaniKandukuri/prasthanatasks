<%= form_with(model: minister, local: true) do |form| %>
  <% if minister.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(minister.errors.count, "error") %> prohibited this minister from being saved:</h2>

      <ul>
        <% minister.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age %>
  </div>

  <div class="field">
    <%= form.label :party %>
    <%= form.text_field :party %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
