<%= form_with(model: add_restaurant_reference_to_rating, local: true) do |form| %>
  <% if add_restaurant_reference_to_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(add_restaurant_reference_to_rating.errors.count, "error") %> prohibited this add_restaurant_reference_to_rating from being saved:</h2>

      <ul>
        <% add_restaurant_reference_to_rating.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Restaurant_id %>
    <%= form.text_field :Restaurant_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
